// // Cria um novo objeto XMLHttpRequest
// let xhttp = new XMLHttpRequest();

// // Define uma função a ser executada sempre que o estado da requisição mudar
// xhttp.onreadystatechange = function() {
//     // Verifica se a requisição foi concluída (readyState == 4) e se foi bem-sucedida (status == 200)
//     if (this.readyState == 4 && this.status == 200) {
//         // Atualiza o conteúdo do elemento com id "demo" com o texto da resposta da requisição
//         document.getElementById("demo").innerHTML = xhttp.responseText;
//     }
// };

// // Configura a requisição para o método GET e a URL especificada
// xhttp.open("GET", "endereço", true);

// // Envia a requisição
// xhttp.send();
