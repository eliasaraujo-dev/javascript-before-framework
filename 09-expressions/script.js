// const groupA = 100;
// const groupB = 300;
// const winner = groupA > groupB ? "Group A Win" : "Group B Win";

// const numbers = [2, 3, 4, 5, 6];
// const total = numbers.filter(numbers => numbers > 4);

// const newWinner = groupA > 50 && "Group A Winner";
// console.log(newWinner);

const groupA = 100;
const groupB = 300;

const winner = groupA > groupB ? "Group A Win" : "Group B Win";

const groupAWin = groupA > 50 && "Group A Winner";

console.log(groupA > 150);